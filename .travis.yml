sudo: required
addons:
  chrome: stable

language: node_js
node_js:
- "8"

branches:
  only:
  - master

before_script:
- npm install -g @angular/cli
- npm run build_app
- npm run build_admin


script:
- npm install -g angular-cli-ghpages
- ng build --prod --base-href '/'
